import styled from "styled-components";

import GridSmall from "./grid-small";

const TableGrid = styled(GridSmall)`
  padding: 0;
  min-width: 1280px;
  transition: 150ms;
`;

export default TableGrid;
